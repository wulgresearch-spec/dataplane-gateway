/**
 * Observability adapters (Doc 27 §4). Contains the production {@code PatternRedactionScanner}
 * ({@code RedactionPort}) — the deterministic, stateless, content-classification reject-residual
 * gate (Doc 27 §15.1, Doc 13 §20). JDK-only; no framework or provider SDK.
 */
package io.reliabilityai.gateway.dataplane.observability.adapter;
