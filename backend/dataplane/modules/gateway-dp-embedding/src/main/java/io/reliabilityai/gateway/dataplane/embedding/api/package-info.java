/**
 * Provider-neutral embedding generation, api layer (AD-030, closes B25).
 *
 * <p>Generates embeddings and nothing else. No index, no similarity, no retrieval — those are B26
 * and beyond, and none of them appears in this module.
 */
package io.reliabilityai.gateway.dataplane.embedding.api;
