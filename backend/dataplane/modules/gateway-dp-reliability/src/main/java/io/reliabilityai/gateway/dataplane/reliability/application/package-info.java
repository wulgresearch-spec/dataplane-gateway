/**
 * The reliability execution use-case (Doc 20 §8): a deterministic, forward-only driver over the
 * candidate list applying retry/failover/circuit/timeout within budgets, fail-closed (RE-INV).
 */
package io.reliabilityai.gateway.dataplane.reliability.application;
