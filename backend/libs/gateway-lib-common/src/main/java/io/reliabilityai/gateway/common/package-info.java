/**
 * Shared primitives (Doc 35 §CmC). Stateless utilities and markers only — no business, domain,
 * framework, or stateful types. Enforced by {@code CommonPackageTest} (ArchUnit).
 */
package io.reliabilityai.gateway.common;
